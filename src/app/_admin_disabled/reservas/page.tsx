export default function AdminReservasPage() {
  return (
    <section className="card">
      <h2 className="font-[var(--font-nunito)] text-xl font-bold">Gestion de reservas</h2>
      <p className="mt-2 text-sm text-brand-warmGray">Listado y estados de reservas (pendiente de conexion DB).</p>
    </section>
  );
}
